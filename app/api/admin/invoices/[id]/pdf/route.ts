import { NextResponse } from "next/server";import { invoiceContext } from "@/lib/invoices/server";import { invoicePdf } from "@/lib/invoices/pdf";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const ctx=await invoiceContext();if(!ctx)return NextResponse.json({error:"Unauthorised"},{status:401});const{id}=await params;const[{data:inv},{data:items},{data:settings}]=await Promise.all([ctx.supabase.from("trackfit_invoices").select("*").eq("id",id).single(),ctx.supabase.from("trackfit_invoice_items").select("*").eq("invoice_id",id).order("position"),ctx.supabase.from("trackfit_invoice_settings").select("*").eq("id",true).single()]);if(!inv||!settings)return NextResponse.json({error:"Not found"},{status:404});const pdf=invoicePdf(inv,items||[],settings as any);await ctx.supabase.from("trackfit_invoice_activity").insert({invoice_id:id,actor_id:ctx.user.id,activity_type:"pdf_downloaded",description:"PDF downloaded"});return new NextResponse(pdf,{headers:{"Content-Type":"application/pdf","Content-Disposition":`inline; filename="${inv.invoice_number}.pdf"`}})}
