import { NextResponse } from "next/server"; import { createSupabaseServerClient } from "@/lib/supabase/server"; import type { PhotoRecord } from "@/types/admin";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const supabase=await createSupabaseServerClient();const{data:{user}}=await supabase.auth.getUser();if(!user||user.app_metadata?.trackfit_admin!==true)return NextResponse.json({error:"Unauthorised"},{status:401});const{data}=await supabase.from("trackfit_enquiries").select("photo_paths").eq("id",id).single();if(!data)return NextResponse.json({error:"Not found"},{status:404});const photos=(data.photo_paths||[]) as PhotoRecord[];const urls:Record<string,string>={};await Promise.all(photos.map(async p=>{const{data:signed}=await supabase.storage.from("trackfit-enquiry-photos").createSignedUrl(p.path,900);if(signed?.signedUrl)urls[p.path]=signed.signedUrl}));return NextResponse.json({urls},{headers:{"Cache-Control":"private, no-store"}})}
