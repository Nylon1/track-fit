import { notFound } from "next/navigation"; import Link from "next/link"; import { requireTrackfitAdmin } from "@/lib/supabase/server"; import LeadEditor from "@/components/admin/LeadEditor"; import LeadPhotos from "@/components/admin/LeadPhotos"; import type { Activity,TrackfitEnquiry } from "@/types/admin";
const label=(x:string)=>x.replaceAll("_"," ");const date=(x:string|null)=>x?new Date(x).toLocaleString("en-GB",{dateStyle:"medium",timeStyle:"short"}):"Not set";
export default async function LeadPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase}=await requireTrackfitAdmin();const[{data},{data:activity=[]}]=await Promise.all([supabase.from("trackfit_enquiries").select("*").eq("id",id).maybeSingle(),supabase.from("trackfit_enquiry_activity").select("*").eq("enquiry_id",id).order("created_at",{ascending:false})]);if(!data)notFound();const lead=data as TrackfitEnquiry;
 const details=[["Reference",lead.reference_number],["Submitted",date(lead.created_at)],["Name",lead.full_name],["Email",lead.email],["Phone",lead.phone],["Preferred contact",label(lead.preferred_contact)],["Postcode",lead.postcode],["Property",label(lead.property_type)],["Track",label(lead.track_type)],["Quantity",lead.track_quantity],["Source",lead.source],["UTM source",lead.utm_source||"—"],["UTM medium",lead.utm_medium||"—"],["UTM campaign",lead.utm_campaign||"—"],["Landing page",lead.landing_page||"—"]];
 const activities=(activity||[]) as Activity[];return <><header className="admin-header"><div><Link href="/admin/leads" className="back-link">← All leads</Link><p className="admin-kicker">{lead.reference_number}</p><h1>{lead.full_name}</h1></div><div><span className={`status status-${lead.status}`}>{label(lead.status)}</span><span className={`priority priority-${lead.priority}`}>{lead.priority}</span></div></header><div className="detail-grid"><div><section className="admin-panel"><h2>Enquiry details</h2><dl className="detail-list">{details.map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl>{lead.customer_notes&&<div className="customer-notes"><h3>Customer notes</h3><p>{lead.customer_notes}</p></div>}</section><section className="admin-panel"><h2>Submitted photographs</h2><LeadPhotos id={lead.id} photos={lead.photo_paths||[]}/></section><section className="admin-panel"><h2>Activity</h2><div className="timeline">{activities.map(a=><article key={a.id}><i/><div><strong>{a.description}</strong><time>{date(a.created_at)}</time></div></article>)}{!activities.length&&<p className="admin-empty">No activity recorded.</p>}</div></section></div><aside className="admin-panel editor-panel"><h2>Manage enquiry</h2><LeadEditor lead={lead}/></aside></div></>}
