import type { BendInstruction } from "@/lib/bay-geometry";import type { MachineConfig } from "./config";
export function machineWarnings(c:MachineConfig,s:BendInstruction[]){const w:string[]=[];if(!c.minimumEndDistance.verified)w.push("Minimum distance from track end: To be confirmed");if(!c.minimumBendSpacing.verified)w.push("Minimum distance between bends: To be confirmed");if(!c.profileOrientation.verified)w.push("Profile orientation: To be confirmed");if(c.minimumBendSpacing.value&&s.length>1&&Math.abs(s[1].position-s[0].position)<c.minimumBendSpacing.value)w.push("Bends are too close together.");return w}
