"use client";
import { useEffect, useRef, useState } from "react"; import { useRouter } from "next/navigation"; import { getSupabaseBrowserClient } from "@/lib/supabase/client";
export default function RealtimeRefresh(){const router=useRouter();const [notice,setNotice]=useState(false);const last=useRef(0);useEffect(()=>{const supabase=getSupabaseBrowserClient();const channel=supabase.channel("trackfit-admin-new-enquiries").on("postgres_changes",{event:"INSERT",schema:"public",table:"trackfit_enquiries"},()=>{if(Date.now()-last.current<1000)return;last.current=Date.now();setNotice(true);router.refresh();setTimeout(()=>setNotice(false),5000)}).subscribe();return()=>{void supabase.removeChannel(channel)}},[router]);return notice?<div className="admin-toast">New TrackFit enquiry received</div>:null}
