"use client";
import Link from "next/link"; import { usePathname, useRouter } from "next/navigation"; import type { ReactNode } from "react"; import { getSupabaseBrowserClient } from "@/lib/supabase/client";
export default function AdminShell({children,email}:{children:ReactNode;email?:string}) { const path=usePathname(); const router=useRouter(); return <div className="admin-app"><aside><Link href="/admin" className="admin-brand">TRACK<span>FIT</span><small>Admin</small></Link><nav><Link className={path==="/admin"?"active":""} href="/admin">Overview</Link><Link className={path.startsWith("/admin/leads")?"active":""} href="/admin/leads">Leads</Link></nav><div className="admin-account"><small>{email}</small><button onClick={async()=>{await getSupabaseBrowserClient().auth.signOut();router.replace("/admin/login");router.refresh();}}>Sign out</button></div></aside><main>{children}</main></div> }
